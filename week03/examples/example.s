    .text
main:
	li	$t1, 42

	# get address

	# calculate offset
	# i * size of value (int = 4, char = 1)

	# add address and offset 

	# store 42 at new address we calculated
    .data
test: 	.word 1, 2, 3, 4